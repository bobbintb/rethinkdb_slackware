name: Delete failed runs
on:
  workflow_dispatch:
jobs:
  delete_runs:
    runs-on: ubuntu-latest
    steps:
      - name: Delete workflow runs
        uses: dmvict/clean-workflow-runs@v1.2.2
        with:
          token: ${{ github.token }}
          repo: bobbintb/slackware_packages
          run_conclusions: |
            failed
